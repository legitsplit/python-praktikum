# Zusammenarbeit mit folgenden Teilnehmern: Adrian Pfumfel
def shortName(name):
    liste = name.split()
    if len(liste) == 1
    return name

    result = ""
    for i in range(len(liste) - 1):
        result += liste[i][0] + "."
    result += " " + liste[len(liste) - 1]
    return result
